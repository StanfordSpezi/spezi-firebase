{
  "name": "spezi-firebase-remote-notifications",
  "version": "0.1.0",
  "description": "Firebase Cloud Messaging (FCM) remote notifications package for Spezi",
  "type": "module",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "scripts": {
    "lint": "eslint src",
    "lint:fix": "eslint src --fix",
    "build": "tsc --noEmitOnError false || true",
    "watch": "tsc --watch",
    "clean": "if [ -f tsconfig.tsbuildinfo ]; then rm tsconfig.tsbuildinfo; fi && if [ -d lib ]; then rm -r lib; fi",
    "test": "env TZ=UTC NODE_OPTIONS=--experimental-vm-modules jest",
    "test:watch": "env TZ=UTC NODE_OPTIONS=--experimental-vm-modules jest --watch",
    "test:ci": "npm run test",
    "test:coverage": "env TZ=UTC NODE_OPTIONS=--experimental-vm-modules jest --coverage",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "spezi",
    "firebase",
    "fcm",
    "notifications",
    "push",
    "messaging"
  ],
  "author": "Stanford University",
  "license": "MIT",
  "dependencies": {
    "firebase-admin": "^12.0",
    "spezi-firebase-utils": "file:../spezi-firebase-utils",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "@types/node": "^20",
    "@types/sinon": "^17.0.3",
    "@typescript-eslint/eslint-plugin": "^7",
    "@typescript-eslint/parser": "^7",
    "chai": "^5.1.1",
    "eslint": "^8.57",
    "eslint-config-google": "^0.14",
    "eslint-config-prettier": "^9",
    "eslint-plugin-import": "^2",
    "eslint-plugin-prettier": "^5",
    "jest": "^29.6.2",
    "sinon": "^18.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5"
  },
  "peerDependencies": {
    "firebase-admin": "^12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
